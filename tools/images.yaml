# ================================================================
#  Image Generation Manifest — Agent Protocol Tech Tree
#
#  Style placeholders: use {style_name} in `prompt` fields.
#  The generate_images.py harness resolves them before sending
#  to the image generation backend.
# ================================================================

styles:
  general: >-
    Flat sprite on a transparent background. Brutalist pixel-art aesthetic
    that mixes old-school 16-bit game sprites with posterized
    high-resolution detail. Limited palette: mostly black and white with
    one bold accent color per icon. Hard 2 px outlines, no gradients,
    no drop shadows, no anti-aliased edges. Crisp and punchy.

  tech_icon: >-
    {general} Tiny square icon, 128×128 px rendered for display at 28×28.
    Chunky, blocky, unmistakably legible at thumbnail size. A single bold
    symbol filling the frame. Maximum contrast. Think "16-bit toolbar
    icon" — every pixel counts.

  actor_icon: >-
    {general} Square character portrait, 256×256 px rendered for display
    at 56×56. Friendlier and more expressive than the tech icons. Retro
    game character feel with clean posterized edges — approachable but
    technical. A little personality: slight tilt, simple expression, or
    posture that conveys the role. Old-school game meets posterized
    high-rez poster mood. Pure white background.


# ════════════════════════════════════════════════════════════════
#  Tech Icons — one per protocol node in the tree view
#  Small, chunky, focused on distinct legibility
# ════════════════════════════════════════════════════════════════

images:

  # ── Tokenizers ────────────────────────────────────────────────
  - filename: icon-tokenizers.png
    category: tech
    data_id: tokenizers
    style: tech_icon
    subject: >-
      The letters "tok" in a chunky mono font, with a small vertical
      divider line suggesting text being split into pieces. Cyan accent.
      Represents text tokenization — splitting strings into model tokens.

  # ── Weight Formats ────────────────────────────────────────────
  - filename: icon-weight-formats.png
    category: tech
    data_id: weight-formats
    style: tech_icon
    subject: >-
      A bold letter "W" with a small grid pattern overlay suggesting
      tensor data. Purple accent. Represents model weight files and
      safe tensor distribution formats.

  # ── Embeddings ────────────────────────────────────────────────
  - filename: icon-embeddings.png
    category: tech
    data_id: embeddings
    style: tech_icon
    subject: >-
      A small cluster of dots arranged in a 3D-perspective grid,
      suggesting a vector space. Magenta accent. Represents embedding
      vectors and semantic similarity.

  # ── Chat Templates ────────────────────────────────────────────
  - filename: icon-chat-templates.png
    category: tech
    data_id: chat-templates
    style: tech_icon
    subject: >-
      Two stacked speech bubbles — one with a small user icon hint,
      one with a small robot/assistant hint. Blue accent. Represents
      multi-turn conversation formatting.

  # ── Inference API ─────────────────────────────────────────────
  - filename: icon-inference-api.png
    category: tech
    data_id: inference-api
    style: tech_icon
    subject: >-
      A blinking command-line terminal prompt — the classic ">_" cursor.
      Bright green accent on black terminal background. Represents
      calling a model API.

  # ── Tool Calling ──────────────────────────────────────────────
  - filename: icon-tool-calling.png
    category: tech
    data_id: tool-calling
    style: tech_icon
    subject: >-
      A bold "f(x)" function symbol — the letter f with parentheses
      and an x. Orange accent. Represents structured tool invocation
      by a language model.

  # ── MCP ───────────────────────────────────────────────────────
  - filename: icon-mcp.png
    category: tech
    data_id: mcp
    style: tech_icon
    subject: >-
      A stylized plug-and-socket connector — two angular brackets
      facing each other "< >" with a spark where they meet. Purple
      accent. Represents the Model Context Protocol connecting clients
      to tool servers.

  # ── MCP Apps ──────────────────────────────────────────────────
  - filename: icon-mcp-apps.png
    category: tech
    data_id: mcp-apps
    style: tech_icon
    subject: >-
      A small application window with a plug symbol inside — combining
      the UI frame with the MCP connector concept. Teal accent.
      Represents interactive UI surfaces for MCP tool servers.

  # ── Standard Tool Surfaces ────────────────────────────────────
  - filename: icon-standard-tool-surfaces.png
    category: tech
    data_id: standard-tool-surfaces
    style: tech_icon
    subject: >-
      Three small stacked icons: a browser window, a terminal prompt,
      and a code file. Gray with white outlines. Represents the
      canonical action surfaces: browser, CLI, and code.

  # ── AGENTS.md ────────────────────────────────────────────────
  - filename: icon-agents-md.png
    category: tech
    data_id: agents-md
    style: tech_icon
    subject: >-
      A bold Markdown hash "#" heading symbol on a small document page.
      Teal accent. Represents a Markdown instruction file that agents
      read on arrival.

  # ── llms.txt ──────────────────────────────────────────────────
  - filename: icon-llms-txt.png
    category: tech
    data_id: llms-txt
    style: tech_icon
    subject: >-
      A small text-file icon with two forward slashes "//" and a few
      ruled lines hinting at a table of contents. Yellow accent.
      Represents an AI-readable site index.

  # ── Agent Skills ──────────────────────────────────────────────
  - filename: icon-agent-skills.png
    category: tech
    data_id: agent-skills
    style: tech_icon
    subject: >-
      A chunky shipping box / package labeled "PKG" with a small
      lightning bolt on the side. Red accent. Represents a portable
      skill package an agent can load.

  # ── Agent Skills Discovery ────────────────────────────────────
  - filename: icon-agent-skills-discovery.png
    category: tech
    data_id: agent-skills-discovery
    style: tech_icon
    subject: >-
      A compass needle over a small globe with a dotted path. Cyan
      accent. Represents finding skills from a predictable well-known
      discovery URL.

  # ── Agent Skills Registries ───────────────────────────────────
  - filename: icon-agent-skills-registries.png
    category: tech
    data_id: agent-skills-registries
    style: tech_icon
    subject: >-
      A directory card catalog with stacked package tiles and a search
      magnifier. Blue accent. Represents searchable npm-style registries
      for skills.

  # ── Agent Loop ────────────────────────────────────────────────
  - filename: icon-agent-loop.png
    category: tech
    data_id: agent-loop
    style: tech_icon
    subject: >-
      A circular arrow forming a loop with a small play/execute symbol
      in the center. Orange accent. Represents the iterative agent
      execution cycle: prompt, act, observe, repeat.

  # ── AgentCard ─────────────────────────────────────────────────
  - filename: icon-agent-card.png
    category: tech
    data_id: agent-card
    style: tech_icon
    subject: >-
      An ID card with tiny capability bullets and an endpoint URL line.
      Teal accent. Represents a well-known JSON descriptor of an
      agent's capabilities and connection info.

  # ── A2A ───────────────────────────────────────────────────────
  - filename: icon-a2a.png
    category: tech
    data_id: a2a
    style: tech_icon
    subject: >-
      Two small square robot-face outlines facing each other with a
      double-headed arrow between them. Cyan accent. Represents
      Agent-to-Agent interoperability.

  # ── UCP ───────────────────────────────────────────────────────
  - filename: icon-ucp.png
    category: tech
    data_id: ucp
    style: tech_icon
    subject: >-
      A bold dollar sign "$" inside a shopping cart outline. Green
      accent. Represents universal agentic commerce — agents that shop.

  # ── Web Bot Auth ──────────────────────────────────────────────
  - filename: icon-web-bot-auth.png
    category: tech
    data_id: web-bot-auth
    style: tech_icon
    subject: >-
      A robot head with a checkmarked signature seal. Gold accent.
      Represents cryptographically signed bot traffic that websites can
      verify.

  # ── Visa TAP ──────────────────────────────────────────────────
  - filename: icon-visa-tap.png
    category: tech
    data_id: visa-tap
    style: tech_icon
    subject: >-
      A payment card tapping against a shield badge. Green accent.
      Represents trusted-agent verification for commerce interactions.

  # ── Agent Audit Traces ────────────────────────────────────────
  - filename: icon-agent-audit.png
    category: tech
    data_id: agent-audit
    style: tech_icon
    subject: >-
      A small clipboard or log sheet with horizontal lines and a
      magnifying glass overlay. Gray with white accents. Represents
      portable audit trails and run records.

  # ── AP2 ───────────────────────────────────────────────────────
  - filename: icon-ap2.png
    category: tech
    data_id: ap2
    style: tech_icon
    subject: >-
      Two linked coins with a signed checkmark receipt. Green accent.
      Represents agent-to-agent payments with mandate verification.

  # ── Signed Intent Mandates ────────────────────────────────────
  - filename: icon-intent-mandates.png
    category: tech
    data_id: intent-mandates
    style: tech_icon
    subject: >-
      A document icon with a signature line and a small seal or stamp
      at the bottom. Gold accent on the seal. Represents cryptographic
      consent and authorization tokens.

  # ── Agent Registries ──────────────────────────────────────────
  - filename: icon-agent-registries.png
    category: tech
    data_id: agent-registries
    style: tech_icon
    subject: >-
      A directory or phonebook icon with small agent/robot symbols
      listed inside. Blue accent. Represents discovery, routing, and
      capability directories for agents.

  # ── Agent Lingua Franca ───────────────────────────────────────
  - filename: icon-agent-lingua.png
    category: tech
    data_id: agent-lingua
    style: tech_icon
    subject: >-
      The letters "abc" with a compression or shorthand symbol — arrows
      pointing inward suggesting compression. Cyan accent. Represents
      token-efficient inter-agent communication language.

  # ── Interoperable Memory ──────────────────────────────────────
  - filename: icon-interoperable-memory.png
    category: tech
    data_id: interoperable-memory
    style: tech_icon
    subject: >-
      A memory card and user silhouette connected by sync arrows. Teal
      accent. Represents portable user context that can move across
      agent products.


# ════════════════════════════════════════════════════════════════
#  Actor Icons — one per actor type in the protocol animations
#  Bigger, friendlier, personality-forward
# ════════════════════════════════════════════════════════════════

  # ── APP (Your App) ──────────────────────────────────────────
  - filename: actor-app.png
    category: actor
    actor_type: app
    style: actor_icon
    subject: >-
      A friendly desktop application window — title bar with three
      dots and a content area showing a blinking cursor. Rounded
      posture. Blue accent. The "protagonist" of every demo.

  # ── LLM (Language Model) ────────────────────────────────────
  - filename: actor-model.png
    category: actor
    actor_type: model
    style: actor_icon
    subject: >-
      A glowing brain or neural-net node cluster inside a square chip
      outline. Radiating small lightning sparks. Purple accent.
      Represents the language model doing inference.

  # ── SRV (Server / Service) ─────────────────────────────────
  - filename: actor-server.png
    category: actor
    actor_type: server
    style: actor_icon
    subject: >-
      A chunky rack server with blinking status LEDs on the front
      panel. Sturdy and boxy. Green accent on the LEDs. Represents
      an API server or tool server.

  # ── AGT (Agent) ─────────────────────────────────────────────
  - filename: actor-agent.png
    category: actor
    actor_type: agent
    style: actor_icon
    subject: >-
      A friendly robot character — boxy head, antenna, simple dot eyes
      and a small determined mouth. One hand raised as if greeting.
      Orange accent. The versatile autonomous agent.

  # ── REPO (Code Repository) ─────────────────────────────────
  - filename: actor-repo.png
    category: actor
    actor_type: repo
    style: actor_icon
    subject: >-
      A folder icon with a small Git branch diagram on the front —
      two dots connected by forking lines. Teal accent on the branch.
      Represents a code repository.

  # ── WEB (Website) ───────────────────────────────────────────
  - filename: actor-website.png
    category: actor
    actor_type: website
    style: actor_icon
    subject: >-
      A browser window with a small globe or world-wide-web icon in
      the address bar and simple horizontal content lines in the body.
      Yellow accent. Represents a documentation website.

  # ── REG (Key Registry) ─────────────────────────────────────
  - filename: actor-registry.png
    category: actor
    actor_type: registry
    style: actor_icon
    subject: >-
      A filing cabinet or ledger book with a large key symbol on the
      front. Secure and official-looking. Gold accent on the key.
      Represents a public-key registry or directory.

  # ── SHOP (Merchant) ────────────────────────────────────────
  - filename: actor-merchant.png
    category: actor
    actor_type: merchant
    style: actor_icon
    subject: >-
      A friendly storefront awning over a shop door — the classic
      market stall look. A small price tag or shopping bag visible.
      Green accent. Represents a merchant in commerce flows.
